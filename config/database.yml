# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
production:
adapter: mysql2
database: redmine
host: localhost
username: root
password: "1234"
encoding: utf8
